.next
node_modules
pnpm-lock.yaml

# 계획/스펙 산문 문서. 임베드된 JSON·표 예시는 저자가 의도적으로 압축한
# 서식이라 prettier의 표준 서식으로 재작성하면 안 된다.
docs/

# 구현 진행 워크스페이스(원장·브리핑·리뷰 패키지). 자신의 .gitignore 로
# 커밋에서는 빠지지만, prettier --check 는 gitignore 를 읽지 않아 작업
# 트리에 있으면 그대로 걸린다.
.superpowers/
